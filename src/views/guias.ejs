<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<style>
    .container {
            background-color: #ffffff;
            border-radius: 16px;
            padding: 40px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            text-align: center;
        }

        h1 {
            color: #0277bd;
            margin-bottom: 20px;
            font-size: 32px;
            font-weight: 600;
        }

        p {
            font-size: 18px;
            margin-bottom: 30px;
            color: #555;
        }

        .scroll-container {
            max-height: 250px;
            overflow-y: auto;
            padding-right: 10px;
            border: 1px solid #ddd;
            border-radius: 12px;
            background-color: #f9f9f9;
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

 /* Estilos del scroll */
 .scroll-container::-webkit-scrollbar {
            width: 12px; /* Ancho del scroll */
        }

        .scroll-container::-webkit-scrollbar-track {
            background: #f9f9f9; /* Fondo del track del scroll */
            border-radius: 12px;
        }

        .scroll-container::-webkit-scrollbar-thumb {
            background-color: #555; /* Color del scroll */
            border-radius: 12px;
            border: 3px solid #f9f9f9; /* Espacio entre el scroll y el track */
        }

        .guide-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .guide-links li {
            margin: 15px 0;
        }

        .guide-links a {
            text-decoration: none;
            font-size: 20px;
            font-weight: 500;
            color: #0288d1;
            padding: 15px 20px;
            border-radius: 8px;
            display: block;
            background-color: #f0f8ff;
            transition: transform 0.3s, box-shadow 0.3s;
        }

        .guide-links a:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 128, 255, 0.2);
        }

</style>
  
<body>
    <%- include('partials/navigation') %> 
    
</body>
</html>